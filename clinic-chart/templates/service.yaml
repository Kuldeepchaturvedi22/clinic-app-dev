apiVersion: v1
kind: Service
metadata:
  name: {{ include "clinic-chart.fullname" . }}
  labels:
    {{- include "clinic-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clinic-chart.selectorLabels" . | nindent 4 }}
